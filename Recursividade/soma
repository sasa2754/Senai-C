#include <stdio.h>
int num, total;

int soma(int num, int c){
    if (c == 0){
        return 1;
    }
    else{
        printf("Total: %i\n", total += c);
        return soma(total, c-1);
    }
}
int main(){
    printf("Digite um numero: ");
    scanf("%i", &num);
    soma(0,num);
    return 0;
}
